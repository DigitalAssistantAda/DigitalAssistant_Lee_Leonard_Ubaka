services:
  # Backend runs locally (not in Docker) due to Supabase IPv6 compatibility
  # Start with: cd backend && source venv/bin/activate && uvicorn main:app --reload --host 0.0.0.0 --port 8000
  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   container_name: digitalassistant-backend
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - DATABASE_URL=postgresql://postgres:senior2026capstone@db.raqcpemoaejgunwebqxs.supabase.co:5432/postgres
  #     - CORS_ORIGINS=http://localhost:3000
  #     - ENVIRONMENT=development
  #     - DEBUG=true
  #   volumes:
  #     - ./backend:/app
  #   networks:
  #     - app-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: digitalassistant-frontend
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:8000
    volumes:
      - ./frontend:/app
      - /app/node_modules
    # depends_on:
    #   - backend
    networks:
      - app-network
    stdin_open: true
    tty: true

networks:
  app-network:
    driver: bridge
